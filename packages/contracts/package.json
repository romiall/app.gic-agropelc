{
  "name": "@gic/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Schémas partagés (appareil + serveur) : enveloppe de commande, protocole de synchronisation, catalogue d'erreurs. ADR-021, 06-offline-sync/02-synchronisation.md.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@gic/domain": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.4",
    "fast-check": "^3.22.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
